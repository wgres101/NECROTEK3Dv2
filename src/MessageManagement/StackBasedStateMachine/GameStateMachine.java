package MessageManagement.StackBasedStateMachine;

public class GameStateMachine {

	//enumerate all states for all services
	//when a new state comes along, push it to the top of the state machine
	//when the state finishes, it will send a message to this
	//this will pop the state and call the state from below
	
	
	
}
