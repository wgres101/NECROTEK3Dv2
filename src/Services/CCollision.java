package Services;

public class CCollision extends ServiceRoot {

	//searches current map hexagon ring for objects with colliders
	//for each collider, check against all colliders
	//if there is a collision, act on it
	//
	
}
