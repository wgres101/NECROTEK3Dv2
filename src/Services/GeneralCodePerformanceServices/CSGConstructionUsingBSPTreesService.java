package Services.GeneralCodePerformanceServices;

//GEMS 5

//INCOMPLETE - REFER TO GEME ARTICLE
/*
public class CSGConstructionUsingBSPTreesService {

	
	class Plane
	{
		
	}
	
	class Face
	{
		
	}
	
	class BackFace
	{
		
	}
	
	public int splitFaceByPlane(Plane plane, Face frontFace, Face backFace)
	{
		Vertex vertex1 = m_points.first();
		Vertex vertex2 = m_points.back();
		
		float fB;
		
		float fA   =  Plane.DistTo(vertex2._xyz);
		
		for(Vertex : m_points)
		{
			vertex1 = vertex;
			fB = Plane.DistTo(vertex1._xyz);
			
			if (fB > EPSILON)
			{
				if (fA < EPSILON)
				{
					float   t = -fA / (fB - fA);
					Vertex = midVertex(vertex1 + (vertex2 - vertex1)*t;
				
				}
				frontFace << midvertex;
				
			}
			
			
			else if (fB <= EPSILON)
			{
				if (fA > EPSILON)
				{
					float t = -fA / (fB - fA);
					Vertex midvertex = vertex1 + (vertex 2 - vertex )*t;
					frontFace >> vertex;
				}
				backFace >> vertex;
				
			
			
				}
			} vertex2 = vertex1;
			  fA = fB;
		}
	
		if (m_points.size() == frontFace.size())
		{
			return 1;
		}
		
		else if (m_points.size() == backFace.size())
			return -1;
		}

		return 0;

		}
}
 */