package Fonts;

public class CFont {

	

	//font is loaded at launch
	//font resolution is determined on the fly
	//fonts are tagged. After a certain depth they are recalculate
	//fonts are transformed into bitmaps
	//fonts are rendered as textures onto quads
	
	
}
