package GamePlayGUIs.MainMenu;

public interface IMap {
/* The interface includes an automap feature similar to the computer games Diablo (RPG), Ultima Online AUTOMAP Add-on, DOOM (FPS), and Solomon’s Keep (iPhone App).
 */
 
}
