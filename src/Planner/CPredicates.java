package Planner;

import java.util.HashMap;

import Planner.PObjects.IRoom;
import Planner.Types.IContainer;
import Planner.Types.ICreature;
import Planner.Types.IFood;
import Planner.Types.IItem;
import Planner.Types.IKey;
import Planner.Types.ILocatable;
import Planner.Types.ILocation;

public class CPredicates {

	/*
	public enum predicate { INROOM, CARRYING, LOCKED, UNLOCKED, PREPARED, INSIDE, OUTROOM
	
	}
	
	public static HashMap<predicate, Boolean> state = new HashMap<predicate, Boolean>();
	
	
	
	
	public static boolean inroom(ILocatable a, ILocation b) {  return true;}//state.put(predicate.INROOM, true); }
	
	public static predicate carrying(ICreature c, IItem i, predicate z) { state.put(predicate.CARRYING, true); }
	
	public static predicate locked(IContainer c) { state.put(predicate.LOCKED, true); }
	
	public static predicate unlocks(IKey k, IContainer c) { state.put(predicate.UNLOCKED, true); } 
	
	public static predicate prepared(IFood f) { state.put(predicate.PREPARED, true); }
	
	public static predicate inside(IItem i, IContainer c) { state.put(predicate.INSIDE, true); }
	*/
	
}
