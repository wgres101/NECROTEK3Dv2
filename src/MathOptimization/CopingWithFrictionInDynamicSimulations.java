package MathOptimization;

//GEMS 3 - Math

public class CopingWithFrictionInDynamicSimulations {

	
	
	// friction vector f_d = { -mu_d*N*v  when v!= 0
	//						{  -mu_d*N*F_t  when v = 0
	
	// F_t is the direction of the tangential component of the net force on the object
	// Friction f_d is now a vector
	// We assume that velocity has no normal component so that the unit vector v is tangent to the contact surface
	// The normal force is the vector N = Nn where n is the unit normal direction
	// mu_d is the coefficent of dynamic friction
	
	
	
}
